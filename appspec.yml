version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/value-app
hooks:
  AfterInstall:
    - location: deploy/install-dependencies.sh
      timeout: 180
  ApplicationStart:
    - location: deploy/start-server.sh
      timeout: 60